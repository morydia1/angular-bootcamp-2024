import { ClassNameDirective } from './class-name.directive';

describe('ClassNameDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassNameDirective();
    expect(directive).toBeTruthy();
  });
});
